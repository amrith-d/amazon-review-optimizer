# Core API dependencies
openai>=1.0.0,<2.0.0
python-dotenv>=1.0.0
tiktoken>=0.5.0
pyyaml>=6.0

# HTTP libraries with SSL compatibility
urllib3>=1.26.0,<2.0.0  # Compatible with LibreSSL on macOS
requests>=2.28.0,<3.0.0
aiohttp>=3.8.0,<4.0.0
httpx>=0.24.0,<1.0.0

# Data processing
pandas>=2.0.0
datasets>=2.14.0
huggingface_hub>=0.16.0

# Async support - removed asyncio as it's built-in
